import { FileText } from "@/lib/icons";
import PlaceholderPage from "@/components/PlaceholderPage";

export default function Admissions() {
  return (
    <PlaceholderPage
      title="Admission Process"
      description="Discover all admission processes, important dates, selection criteria, and expert tips to succeed in your applications."
      icon={FileText}
    />
  );
}
